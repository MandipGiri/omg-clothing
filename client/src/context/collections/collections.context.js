import { createContext } from "react";
import { Collections } from "../../utilites/constants/CollectionItemsConstant";

const CollectionContext = createContext(Collections);

export default CollectionContext;
